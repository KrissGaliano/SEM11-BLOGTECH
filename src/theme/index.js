import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // colores
  // fuentes
  // espacios
  // sombres
  palette: {
    primary: {
      light: "#87D0C5 ",
      main: "#A3E38F ",
      dark: "#3549C6",
    },
    secondary: {
      main: "#DD7094",
    },
  },
});

export default theme;
